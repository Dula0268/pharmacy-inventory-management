<UserControl x:Class="PharmacyInventory.Views.AdminTabs.SearchProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:PharmacyInventory.Models"
             mc:Ignorable="d" d:DesignHeight="520" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Width="360" Margin="0,0,8,0" VerticalAlignment="Center" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox Width="160" Margin="0,0,8,0" SelectedValuePath="Content" SelectedValue="{Binding SelectedFilter, Mode=TwoWay}">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Medicine" />
                <ComboBoxItem Content="Grocery" />
            </ComboBox>
            <Button Width="100" Content="Search" Command="{Binding SearchCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Results}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80" />
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*" />
                <DataGridTextColumn Header="Qty" Binding="{Binding QuantityOnHand}" Width="80" />
                <DataGridTextColumn Header="Exp Date" Binding="{Binding ExpDate}" Width="120" />
                <DataGridTextColumn Header="Buying" Binding="{Binding BuyingPrice, StringFormat='LKR {0:N2}'}" Width="100" />
                <DataGridTextColumn Header="Selling" Binding="{Binding SellingPrice, StringFormat='LKR {0:N2}'}" Width="100" />
                <DataGridTextColumn Header="Distributor" Binding="{Binding Distributor}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
