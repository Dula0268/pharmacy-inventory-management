<UserControl x:Class="PharmacyInventory.Views.AdminTabs.ExpirationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="900">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top controls -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock VerticalAlignment="Center" Text="Near expiry filter:" Margin="0,0,10,0"/>

            <ComboBox Width="160"
                      SelectedValuePath="Tag"
                      SelectedValue="{Binding SelectedMonths, Mode=TwoWay}">
                <ComboBoxItem Content="1 month" Tag="1"/>
                <ComboBoxItem Content="2 months" Tag="2"/>
                <ComboBoxItem Content="3 months" Tag="3"/>
            </ComboBox>

            <Button Width="100"
                    Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Margin="12,0,0,0"/>
        </StackPanel>

        <!-- Results -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Results}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="90"/>
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding QuantityOnHand}" Width="80"/>
                <DataGridTextColumn Header="Exp Date" Binding="{Binding ExpDate}" Width="130"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
