<UserControl x:Class="PharmacyInventory.Views.AdminTabs.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:PharmacyInventory.Models"
             mc:Ignorable="d" d:DesignHeight="520" d:DesignWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Header="Add User" Padding="8">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock VerticalAlignment="Center" Width="120" Text="Username:" />
                    <TextBox Width="260" Text="{Binding NewUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Width="120" Text="Password:" />
                    <PasswordBox Width="200" x:Name="pwdBox" />
                    <TextBlock Width="12" />
                    <TextBlock VerticalAlignment="Center" Width="80" Text="Role:" />
                    <ComboBox Width="120" SelectedValuePath="Tag" SelectedValue="{Binding SelectedRole, Mode=TwoWay}">
                        <ComboBoxItem Tag="{x:Static models:UserRole.Admin}">Admin</ComboBoxItem>
                        <ComboBoxItem Tag="{x:Static models:UserRole.Cashier}">Cashier</ComboBoxItem>
                    </ComboBox>

                    <Button Content="Add" Width="100" Margin="12,0,0,0" Click="OnAddClick" HorizontalAlignment="Right" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,8,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="200" />
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120" />
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive, Mode=TwoWay}" Width="80" />
                    <DataGridTemplateColumn Header="Actions" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
 
