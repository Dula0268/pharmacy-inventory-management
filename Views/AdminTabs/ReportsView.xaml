<UserControl x:Class="PharmacyInventory.Views.AdminTabs.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Daily report -->
        <GroupBox Header="Daily Report" Padding="8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center" Width="80" Text="Date:" />
                <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" />
                <Button Content="Load" Command="{Binding LoadDailyCommand}" Margin="12,0,0,0" />
                <TextBlock Text="Total:" Margin="24,0,0,0" VerticalAlignment="Center" />
                <TextBlock Text="{Binding DailyTotal}" Margin="6,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding DailySales}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,8,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="180" />
                <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Monthly report -->
        <GroupBox Header="Monthly Report" Grid.Row="2" Padding="8">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Width="80" Text="Year:" />
                    <ComboBox Width="100" SelectedValue="{Binding SelectedYear, Mode=TwoWay}">
                        <!-- simple year range -->
                        <ComboBoxItem Content="2024" />
                        <ComboBoxItem Content="2025" />
                        <ComboBoxItem Content="2026" />
                    </ComboBox>
                    <TextBlock VerticalAlignment="Center" Width="80" Text="Month:" Margin="12,0,0,0" />
                    <ComboBox Width="140" SelectedValue="{Binding SelectedMonth, Mode=TwoWay}">
                        <ComboBoxItem Content="1" />
                        <ComboBoxItem Content="2" />
                        <ComboBoxItem Content="3" />
                        <ComboBoxItem Content="4" />
                        <ComboBoxItem Content="5" />
                        <ComboBoxItem Content="6" />
                        <ComboBoxItem Content="7" />
                        <ComboBoxItem Content="8" />
                        <ComboBoxItem Content="9" />
                        <ComboBoxItem Content="10" />
                        <ComboBoxItem Content="11" />
                        <ComboBoxItem Content="12" />
                    </ComboBox>
                    <Button Content="Load" Command="{Binding LoadMonthlyCommand}" Margin="12,0,0,0" />
                    <TextBlock Text="Total:" Margin="24,0,0,0" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding MonthlyTotal}" Margin="6,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <TextBlock FontWeight="SemiBold" Margin="0,8,0,4" Text="Daily Totals" />
                <DataGrid ItemsSource="{Binding DailyTotals}" AutoGenerateColumns="False" CanUserAddRows="False" Height="160">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Day" Binding="{Binding Day}" Width="80" />
                        <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock FontWeight="SemiBold" Margin="0,8,0,4" Text="Top Products" />
                <DataGrid ItemsSource="{Binding TopProducts}" AutoGenerateColumns="False" CanUserAddRows="False" Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding DisplayName}" Width="*" />
                        <DataGridTextColumn Header="Qty" Binding="{Binding TotalQty}" Width="100" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
 
