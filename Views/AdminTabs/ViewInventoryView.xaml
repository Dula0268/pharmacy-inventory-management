<UserControl x:Class="PharmacyInventory.Views.AdminTabs.ViewInventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="520" d:DesignWidth="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Width="100" Content="Refresh" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="90" />
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*" />
                <DataGridTextColumn Header="Qty" Binding="{Binding QuantityOnHand}" Width="80" />
                <DataGridTextColumn Header="Buying" Binding="{Binding BuyingPrice, StringFormat='LKR {0:N2}'}" Width="100" />
                <DataGridTextColumn Header="Selling" Binding="{Binding SellingPrice, StringFormat='LKR {0:N2}'}" Width="100" />
                <DataGridTextColumn Header="Mfg Date" Binding="{Binding MfdDate}" Width="120" />
                <DataGridTextColumn Header="Exp Date" Binding="{Binding ExpDate}" Width="120" />
                <DataGridTextColumn Header="Distributor" Binding="{Binding Distributor}" Width="200" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
