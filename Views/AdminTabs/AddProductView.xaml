<UserControl x:Class="PharmacyInventory.Views.AdminTabs.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:PharmacyInventory.Models"
             mc:Ignorable="d"
             d:DesignHeight="780" d:DesignWidth="980">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <ScrollViewer Padding="12">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="920">
            <TextBlock FontSize="18" FontWeight="Bold" Text="Add Product" Margin="0,0,0,12" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock VerticalAlignment="Center" Width="120" Text="Product Type:" />
                <ComboBox Width="200" SelectedValuePath="Tag" SelectedValue="{Binding SelectedType, Mode=TwoWay}">
                    <ComboBoxItem Tag="{x:Static models:ProductType.Medicine}" Content="Medicine" />
                    <ComboBoxItem Tag="{x:Static models:ProductType.Grocery}" Content="Grocery" />
                </ComboBox>
            </StackPanel>

            <!-- Medicine fields -->
            <StackPanel x:Name="MedicinePanel" Visibility="{Binding IsMedicine, Converter={StaticResource BoolToVis}}" Margin="0,0,0,12">
                <TextBlock FontWeight="SemiBold" Text="Medicine Details" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Category:" />
                    <TextBox Width="220" Text="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Generic Name:" />
                    <TextBox Width="220" Text="{Binding GenericName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Brand Name:" />
                    <TextBox Width="220" Text="{Binding BrandName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Strength:" />
                    <TextBox Width="220" Text="{Binding Strength, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Dosage Form:" />
                    <TextBox Width="220" Text="{Binding DosageForm, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Batch No:" />
                    <TextBox Width="220" Text="{Binding BatchNo, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="MFD:" />
                    <DatePicker Width="220" SelectedDate="{Binding MedicineMfdDate, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="EXP:" />
                    <DatePicker Width="220" SelectedDate="{Binding MedicineExpDate, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Packing:" />
                    <TextBox Width="220" Text="{Binding Packing, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="No of Units:" />
                    <TextBox Width="220" Text="{Binding NoOfUnits, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Unit Price:" />
                    <TextBox Width="220" Text="{Binding UnitPrice, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Units/Pack:" />
                    <TextBox Width="220" Text="{Binding UnitsPerPack, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="No of Packs:" />
                    <TextBox Width="220" Text="{Binding NoOfPacks, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Pack Price:" />
                    <TextBox Width="220" Text="{Binding PackPrice, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Total Value:" />
                    <TextBox Width="220" Text="{Binding TotalValue, Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>

            <!-- Grocery fields -->
            <StackPanel x:Name="GroceryPanel" Visibility="{Binding IsGrocery, Converter={StaticResource BoolToVis}}" Margin="0,0,0,12">
                <TextBlock FontWeight="SemiBold" Text="Grocery Details" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Item Type:" />
                    <TextBox Width="220" Text="{Binding ItemType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Brand:" />
                    <TextBox Width="220" Text="{Binding Brand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Speciality:" />
                    <TextBox Width="220" Text="{Binding Speciality, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Size:" />
                    <TextBox Width="220" Text="{Binding Size, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Price:" />
                    <TextBox Width="220" Text="{Binding Price, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Count:" />
                    <TextBox Width="220" Text="{Binding Count, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Center" Text="Total:" />
                    <TextBox Width="220" Text="{Binding Total, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="EXD:" />
                    <DatePicker Width="220" SelectedDate="{Binding GroceryExdDate, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Center" Text="MFD:" />
                    <DatePicker Width="220" SelectedDate="{Binding GroceryMfdDate, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Width="120" VerticalAlignment="Top" Text="Out Colour:" />
                    <TextBox Width="220" Text="{Binding OutColour, Mode=TwoWay}" />
                    <TextBlock Width="30" />
                    <TextBlock Width="120" VerticalAlignment="Top" Text="Note:" />
                    <TextBox Width="460" Height="60" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding Note, Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,12">
                <Button Width="160" Height="30" Margin="0,0,8,0" Content="Import Medicine Excel" Command="{Binding ImportMedicineCommand}" />
                <Button Width="160" Height="30" Content="Import Grocery Excel" Command="{Binding ImportGroceryCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
                <Button Width="120" Height="30" Margin="0,0,8,0" Content="Save" Command="{Binding SaveCommand}" />
                <Button Width="120" Height="30" Content="Clear" Command="{Binding ClearCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
